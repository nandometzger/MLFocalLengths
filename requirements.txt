# Inference and training dependencies.
#
#   pip install -r requirements.txt
#
# Version floors are security floors, not API requirements: torch < 2.6 loads
# checkpoints with an unrestricted pickle, and the older Pillow/opencv/urllib3
# releases pinned by the previous conda export carried known CVEs.
#
# The previous version of this file was a `conda list --explicit` dump of a
# linux-64 environment (321 pinned builds, most of them irrelevant transitive
# system libraries). It could not be installed with pip and could not be
# installed by conda on any other platform.

torch>=2.6
torchvision>=0.21

numpy>=1.26
opencv-python-headless>=4.10   # headless: no GUI/X11 stack needed on servers
Pillow>=10.3
matplotlib>=3.8
tqdm>=4.66

# EXIF and RAW handling
piexif>=1.1.3
exifread>=3.0
rawpy>=0.21

# Pretrained weight download
gdown>=5.1

# Training only
h5py>=3.11
scikit-learn>=1.5
wandb>=0.17
